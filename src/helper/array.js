/**
 * @returns {boolean} Returns true if the length of the array
 * @param {*} array
 * @param {integer} array.length Array length
 */
export const isArrayEmptyOrUndefined = (array = []) => array.length === 0;