import styled from 'styled-components';

const StyledImage = styled.img`
    margin: 0.2rem;
    width: auto;
    max-width: 100%;
    height: 7rem;
    object-fit: cover;
`;

export default StyledImage;